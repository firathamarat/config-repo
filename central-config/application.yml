#common configuration shared between all applications
configserver:
  name: Docker JHipster Registry
  status: Connected to the JHipster Registry running in Docker

jhipster:
  security:
    authentication:
      jwt:
        base64-secret: Mjk5ODkyNWMyY2IxYzgxMzFmMWFkY2Y3NmRiNTExNWRkYTQ3NDViMzVhYmJhOGM4M2Q3ZGFmM2QyMjNiYTM3MjI3Y2FjNmFmODRmNzA4YzFiMzNkOWFmYzZkZTc0ZTBmNTYyYw==
  logging:
    logstash: # forward logs to ELK
      enabled: true
      host: jhipster-logstash
  metrics:
    logs: # report metrics in the logs
      enabled: true
      report-frequency: 60 # in seconds

eureka:
  client:
    service-url:
      defaultZone: https://admin:${jhipster.registry.password}@discovery.meanerror.com/eureka
      # defaultZone: http://admin:${jhipster.registry.password}@jhipster-registry:8761/eureka/