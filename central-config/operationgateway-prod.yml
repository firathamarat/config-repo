zuul:
  routes:
    driver:
      path: /driver/**
      serviceId: driver
    operation:
      path: /operation/**
      serviceId: operation
      # url: operation
      # strip-prefix: false

ribbon:
  ReadTimeout: 20000
  ConnectTimeout: 20000