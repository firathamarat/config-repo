zuul:
  routes:
    driver:
      path: /driver/**
      serviceId: driver
    order:
      path: /order/**
      serviceId: order
    operation:
      path: /operation/**
      serviceId: operation
      # url: operation
      # strip-prefix: false

ribbon:
  ReadTimeout: 10000      