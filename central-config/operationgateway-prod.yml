zuul:
  routes:
    driver:
      path: /services/driver/**
      serviceId: driver
    order:
      path: /services/order/**
      serviceId: order
    operation:
      path: /services/operation/**
      serviceId: operation
      # url: operation
      # strip-prefix: false

ribbon:
  ReadTimeout: 10000      