zuul:
  routes:
    driver:
      path: /driver/**
      serviceId: driver
    operation:
      path: /operation/**
      serviceId: operation
      # url: operation
      # strip-prefix: false

driver:
  ribbon:
    ReadTimeout: 60000
    ConnectTimeout: 60000

operation:
  ribbon:
    ReadTimeout: 60000
    ConnectTimeout: 60000