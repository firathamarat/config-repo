zuul:
  routes:
    driver:
      path: /driver/**
      serviceId: driver
      strip-prefix: false
    operation:
      path: /operation/**
      serviceId: operation
      strip-prefix: false
      # url: operation
      # strip-prefix: false

ribbon:
  ReadTimeout: 20000
  ConnectTimeout: 20000