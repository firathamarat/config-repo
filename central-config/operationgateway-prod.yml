zuul:
  routes:
    driver-app:
      path: /driver/**
      serviceId: driver
    operation-app:
      path: /operation/**
      serviceId: operation
      # url: operation
      # strip-prefix: false
