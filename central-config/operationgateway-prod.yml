zuul:
  routes:
    driver:
      path: /driver/**
      url: https://meanerror.com/services/driver
    operation:
      path: /operation/**
      url: https://meanerror.com/services/operation
      # url: operation
      # strip-prefix: false
