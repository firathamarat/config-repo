zuul:
  host:
    connect-timeout-millis: 20000
    socket-timeout-millis: 60000
  routes:
    driver:
      path: /driver/**
      serviceId: driver
    
    operation:
      path: /operation/**
      serviceId: operation
      # url: operation
      # strip-prefix: false