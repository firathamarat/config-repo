zuul:
  stripPrefix: true
  ignoredServices: '*'
  routes:
    order:
      path: /services/order/api/**
      serviceId: order-api
  retryable: true

order:
  ribbon:
    ConnectTimeout: 10000
    ReadTimeout: 10000