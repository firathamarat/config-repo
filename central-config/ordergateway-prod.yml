zuul:
  routes:
    order:
      path: /order/**
      serviceId: order
      stripPrefix: true
    customer:
      path: /customer/**
      serviceId: customer
      stripPrefix: true
      # url: customer
      # strip-prefix: false

hystrix:
  command:
    order:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 15000


customer:
  ribbon:
    ReadTimeout: 15000


hystrix:
  command:
    customer:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 15000


order:
  ribbon:
    ReadTimeout: 15000