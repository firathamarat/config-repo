zuul:
  routes:
    order:
      path: /order/**
      serviceId: order
    customer:
      path: /customer/**
      serviceId: customer
      # url: customer
      # strip-prefix: false

hystrix:
  command:
    order:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 15000
customer:
  ribbon:
    ReadTimeout: 15000