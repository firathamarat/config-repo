zuul:
  routes:
    order:
      path: /order/**
      serviceId: order
    
    customer:
      path: /customer/**
      serviceId: customer
      # url: customer
      # strip-prefix: false

ribbon:
  ReadTimeout: 10000

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 60000  