zuul:
  routes:
    order:
      path: /services/order/**
      serviceId: order
    customer:
      path: /services/customer/**
      serviceId: customer
      # url: customer
      # strip-prefix: false