zuul:
  routes:
    order:
      path: /order/**
      serviceId: order
      strip-prefix: false
    customer:
      path: /customer/**
      serviceId: customer
      # url: customer
     strip-prefix: false

ribbon:
  ReadTimeout: 20000
  ConnectTimeout: 20000