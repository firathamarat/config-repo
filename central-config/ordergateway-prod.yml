zuul:
  routes:
    order:
      path: /order/**
      serviceId: order
    customer:
      path: /customer/**
      serviceId: customer
      # url: customer
      # strip-prefix: false

order:
  ribbon:
    ReadTimeout: 60000
    ConnectTimeout: 60000