zuul:
  routes:
    order:
      path: /order/**
      url: https://meanerror.com/services/order
    customer:
      path: /customer/**
      url: https://meanerror.com/services/customer
      # url: customer
      # strip-prefix: false
