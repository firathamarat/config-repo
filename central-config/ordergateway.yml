zuul:
  routes:
    order:
      path: /order/**
      serviceId: order
    
    customer:
      path: /customer/**
      serviceId: customer
      # url: customer
      # strip-prefix: false

# zuul:
#   routes:
#     ordergateway:
#       path: /ordergateway/**
#       serviceId: ordergateway
        