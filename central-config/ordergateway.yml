zuul:
  routes:
    order:
      path: /services/order/**
      serviceId: order
    
    customer:
      path: /services/customer/**
      serviceId: custommer
      # url: order
      # strip-prefix: false
