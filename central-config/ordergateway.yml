zuul:
  host:
    connect-timeout-millis: 20000
    socket-timeout-millis: 60000
  routes:
    order:
      path: /order/**
      serviceId: order
    
    customer:
      path: /customer/**
      serviceId: customer
      # url: customer
      # strip-prefix: false