zuul:
  routes:
    order:
      path: order/**
      serviceId: order
    
    customer: customer/**
      serviceId: custommer
      # url: order
      # strip-prefix: false

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 60000
  command:
    default:
      execution:
        timeout:
          enable: false

ribbon:
  eager-load:
    enabled: true
  eureka:
    enabled: true
  ConnectTimeout: 5000
  ReadTimeout: 120000 #2 minutes