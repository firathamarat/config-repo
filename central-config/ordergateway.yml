zuul:
  host:
    connect-timeout-millis: 20000
    socket-timeout-millis: 60000
  routes:
    order:
      path: /services/order/**
      serviceId: order
    
    customer:
      path: /services/customer/**
      serviceId: customer
      # url: customer
      # strip-prefix: false